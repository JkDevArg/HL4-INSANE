# Base de la API. Por defecto /api (nginx hace proxy /api -> platform-api:8000).
# Para desarrollo local apuntando directo al backend usa, por ejemplo:
#   NEXT_PUBLIC_API_BASE=http://localhost:8000
NEXT_PUBLIC_API_BASE=/api
