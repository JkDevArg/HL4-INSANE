# Solución — crypto-CAMBIAME-NN (TEMPLATE)

> Reemplaza con la solución real + script de explotación.

## Vulnerabilidad central
CAMBIAME (padding oracle AES-CBC, ECB cut-and-paste, nonce reuse...).

## Pasos / script
1. ...
2. Flag: `flag{EJEMPLO}`.

## Nota anti-cheat
El reto se sirve **por red** (`nc host 9999`): no hay binario descargable, así
que no se puede analizar offline ni compartir el "artefacto". Cada equipo tiene
su instancia con su clave/flag únicas. La flag es dinámica por equipo
(`ARCHITECTURE §4`); enviar la de otro dispara `cheat_flag_share`.
