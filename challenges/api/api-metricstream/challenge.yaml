id: api-metricstream
category: api
name: "MetricStream Infrastructure API"
difficulty: insane
type: per-team
serve: http
flag_via: flag-service
points: 500
ports: [5003]
siem: true
description: >
  "MetricStream", API REST para monitoreo de infraestructura. El sistema tiene
  endpoints internos protegidos por autenticación de servicio. Se supone que el
  proxy inverso filtra las cabeceras internas antes de llegar a la aplicación,
  pero algo en la arquitectura no está bien implementado.
  Endpoint: http://<host>:5003
