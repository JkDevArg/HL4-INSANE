# api-grpc-04 — SilentChannel. Parametrizable por equipo: TEAM_ID + FLAG + TEAM_N + CHAL_NET.
#   Lanzado por infra/launch-team-challenges.sh:
#     TEAM_ID=team_03 TEAM_N=3 FLAG="HL4{...}" CHAL_NET=ctf_team_03 \
#       docker compose -p ctf_team_03_api_grpc_04 up -d --build
#
# AISLAMIENTO: vive SOLO en la red del equipo (172.30.N.0/24) en .23:8080, sin
# publicar puertos al host. El servidor gRPC (HTTP/2) escucha en 8080 interno.
# Accesible por la VPN del equipo vía nftables.
networks:
  team_net:
    external: true
    name: ${CHAL_NET:-ctf_local}

services:
  api:
    build: .
    image: api-grpc-04
    environment:
      FLAG: "${FLAG:-HL4{EJEMPLO_LOCAL}}"
      TEAM_ID: "${TEAM_ID:-team_local}"
      CHALLENGE_ID: "api-grpc-04"
      COLLECTOR_URL: "${COLLECTOR_URL:-http://collector:9000}"
    networks:
      team_net:
        ipv4_address: 172.30.${TEAM_N:-99}.23
    restart: unless-stopped
